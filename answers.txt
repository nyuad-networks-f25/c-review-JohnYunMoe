(1) The error is that the main.c file is empty, the fix is to add a couple lines of code that do something or to just return 0.
(2) By making a .gitignore file and adding the name of the executable file in that file. 
(3) 
